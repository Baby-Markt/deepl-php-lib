language: php

sudo: false

php:
  - 5.4
  - 5.6
  - 7.0
  - 7.1
  - 7.2
  - nightly

matrix:
  include:
    - php: 5.3
      dist: precise

cache:
  directories:
    - $HOME/.composer/cache

before_script:
  - composer install

script:
  - composer test